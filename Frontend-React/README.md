# React + Vite



- Add state for username, password, and error messages
- Implement handleLogin function with axios POST
- Store access and refresh tokens in localStorage
- Display server-side errors below form inputs

exam details :
- store startAt in localStorage when exam starts
- calculate timeLeft on page reload based on startAt
- record endTime when timer finishes
- display timer with hours:minutes:seconds and color warning for last 10 minutes
- use Tailwind for styling

designed with MUI such as :
-Alert
-Sidebar